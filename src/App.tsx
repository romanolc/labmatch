import { useEffect, useMemo, useState } from "react";
import {
  Accessibility, Activity, AlertTriangle, ArrowRight, ArrowUpRight, BadgeCheck, Bell, Compass,
  Bookmark, BookOpen, Building2, CalendarCheck2, CalendarDays, Check, CheckCircle2,
  ChevronDown, ChevronRight, Circle, Clock3, Contrast, Cpu, ExternalLink, Eye,
  FolderKanban, Globe2, Heart, Home, Laptop, Layers3, Lightbulb, Loader2, LocateFixed,
  LockKeyhole, LogOut, MapPin, MapPinned, Menu, MessageCircle, Moon, MoreHorizontal,
  Plus, Printer, QrCode, Route, Search, Send, Settings2, Share2, ShieldCheck, SlidersHorizontal,
  Sparkles, Sun, TicketCheck, Trash2, Type, UserPlus, UserRound, UsersRound, Volume2,
  Wrench, X, Zap, ZoomIn, ZoomOut
} from "lucide-react";
import { Toaster, toast } from "sonner";

type Page = "home" | "explore" | "map" | "reservas" | "community" | "projects" | "events" | "notifications" | "profile" | "preferences";
type LabStatus = "available" | "busy" | "reserved" | "maintenance";

type Lab = {
  id: number; name: string; category: string; building: string; floor: string; capacity: number;
  occupancy: number; status: LabStatus; match: number; color: string; description: string;
  equipment: string[]; resources: string[]; accessibility: string[]; hours: string; projects: string[];
};

type Reservation = { id: string; labId: number; date: string; time: string; duration: string; objective: string; people: number; status: "confirmed" | "checked" | "cancelled" };
type Post = { id: number; author: string; role: string; initials: string; type: string; title: string; body: string; tags: string[]; likes: number; comments: number; time: string; liked?: boolean; saved?: boolean };

const labs: Lab[] = [
  { id: 1, name: "Laboratório de Prototipagem", category: "Prototipagem", building: "CIMATEC 2", floor: "Térreo · Ala Norte", capacity: 20, occupancy: 6, status: "available", match: 94, color: "#3c82f6", description: "Bancadas modulares para transformar ideias em protótipos funcionais com agilidade.", equipment: ["Arduino", "Raspberry Pi", "Impressora 3D", "Sensores"], resources: ["Bancadas antiestáticas", "Ferramentas manuais", "Exaustão localizada"], accessibility: ["Entrada acessível", "Mesa adaptada", "Baixo ruído"], hours: "Seg — Sex · 07:00 — 22:00", projects: ["IRIS", "EcoTrack"] },
  { id: 2, name: "Lab de Eletrônica Aplicada", category: "Eletrônica", building: "CIMATEC 1", floor: "2º andar · Sala 204", capacity: 12, occupancy: 9, status: "busy", match: 88, color: "#f59e0b", description: "Instrumentação, sensores e circuitos para experimentos com precisão de bancada.", equipment: ["Osciloscópios", "Arduino", "Fontes DC", "Multímetros"], resources: ["Bancadas antiestáticas", "Estação de solda", "Projetor"], accessibility: ["Sinalização visual", "Iluminação adequada"], hours: "Seg — Sex · 08:00 — 21:00", projects: ["AeroSense"] },
  { id: 3, name: "Arena de Robótica", category: "Robótica", building: "CIMATEC 3", floor: "1º andar · Galpão B", capacity: 30, occupancy: 14, status: "available", match: 86, color: "#8b5cf6", description: "Espaço aberto para testar robôs móveis, braços articulados e sistemas autônomos.", equipment: ["Kits robóticos", "Raspberry Pi", "LIDAR", "Câmeras"], resources: ["Arena de testes", "Rede dedicada", "Ferramentas"], accessibility: ["Entrada acessível", "Iluminação adequada"], hours: "Seg — Sáb · 07:00 — 20:00", projects: ["RoboCIMATEC", "Navi"] },
  { id: 4, name: "Estúdio de Fabricação Digital", category: "Fabricação", building: "CIMATEC 4", floor: "Térreo · Fab Lab", capacity: 18, occupancy: 4, status: "available", match: 82, color: "#06b6d4", description: "Fabricação digital com máquinas de corte, impressão e acabamento de alta qualidade.", equipment: ["Impressora 3D", "CNC", "Cortadora laser", "Scanner 3D"], resources: ["Materiais diversos", "Cabine de pintura", "Exaustão"], accessibility: ["Entrada acessível", "Mesa adaptada"], hours: "Seg — Sex · 08:00 — 18:00", projects: ["Forma", "BioShape"] },
  { id: 5, name: "Núcleo de Computação", category: "Computação", building: "CIMATEC 1", floor: "3º andar · Sala 306", capacity: 24, occupancy: 17, status: "reserved", match: 79, color: "#64748b", description: "Ambiente silencioso para desenvolvimento, simulação e experimentação em software.", equipment: ["Workstations", "GPU dedicada", "Monitores 4K", "VR"], resources: ["Wi-Fi 6", "Quadro digital", "Climatização"], accessibility: ["Baixo ruído", "Iluminação adequada"], hours: "Seg — Sex · 07:00 — 22:00", projects: ["IRIS", "DataLab"] },
  { id: 6, name: "Laboratório de IA & Visão", category: "Inteligência Artificial", building: "CIMATEC 2", floor: "4º andar · Sala 412", capacity: 16, occupancy: 8, status: "available", match: 77, color: "#ec4899", description: "Computação de alto desempenho para modelos de visão, dados e inteligência artificial.", equipment: ["GPU NVIDIA", "Câmeras industriais", "Kinect", "Sensores"], resources: ["Rede dedicada", "Blackout", "Quadro digital"], accessibility: ["Entrada acessível", "Baixo ruído"], hours: "Seg — Sex · 08:00 — 22:00", projects: ["IRIS", "VisioSafe"] },
  { id: 7, name: "Oficina de Materiais", category: "Materiais", building: "CIMATEC 3", floor: "Térreo · Ala Leste", capacity: 10, occupancy: 10, status: "maintenance", match: 71, color: "#ef4444", description: "Caracterização e preparação de materiais para pesquisa aplicada e experimentação.", equipment: ["Prensa", "Furadeira de bancada", "Forno", "Ferramentas"], resources: ["EPIs", "Bancadas reforçadas", "Exaustão"], accessibility: ["Sinalização visual"], hours: "Seg — Sex · 08:00 — 18:00", projects: ["BioShape"] },
  { id: 8, name: "Sala de Projetos Colaborativos", category: "Colaboração", building: "CIMATEC 4", floor: "2º andar · Hub", capacity: 24, occupancy: 11, status: "available", match: 68, color: "#10b981", description: "Sala flexível para equipes que precisam pensar, construir e apresentar juntas.", equipment: ["Monitores", "Webcams", "Áudio", "Notebooks"], resources: ["Quadro branco", "Videoconferência", "Café"], accessibility: ["Entrada acessível", "Mesa adaptada", "Baixo ruído"], hours: "Seg — Sáb · 07:00 — 22:00", projects: ["Navi", "EcoTrack"] },
  { id: 9, name: "Laboratório de Manufatura", category: "Manufatura", building: "CIMATEC 3", floor: "2º andar · Galpão A", capacity: 28, occupancy: 21, status: "busy", match: 65, color: "#f97316", description: "Máquinas e dispositivos para testar processos de manufatura e automação.", equipment: ["CNC", "Robôs industriais", "Sensores", "CLP"], resources: ["Ponte rolante", "EPIs", "Área de testes"], accessibility: ["Entrada acessível", "Sinalização visual"], hours: "Seg — Sex · 07:00 — 19:00", projects: ["RoboCIMATEC"] },
  { id: 10, name: "Estúdio de Experiência", category: "UX & Design", building: "CIMATEC 2", floor: "1º andar · Sala 118", capacity: 14, occupancy: 5, status: "available", match: 63, color: "#14b8a6", description: "Ambiente para pesquisa com usuários, prototipação de interfaces e workshops.", equipment: ["Câmeras", "Eye tracker", "Tablets", "Áudio"], resources: ["Sala de observação", "Iluminação ajustável", "Quadro branco"], accessibility: ["Entrada acessível", "Iluminação adequada", "Baixo ruído"], hours: "Seg — Sex · 08:00 — 20:00", projects: ["Navi", "IRIS"] }
];

const initialReservations: Reservation[] = [{ id: "LM-2408", labId: 1, date: "18 set 2026", time: "14:00 — 16:00", duration: "2 horas", objective: "Protótipo de sensor de qualidade do ar", people: 4, status: "confirmed" }];
const buildings = ["Todos os prédios", "CIMATEC 1", "CIMATEC 2", "CIMATEC 3", "CIMATEC 4"];
const navItems: { id: Page; label: string; icon: typeof Home }[] = [
  { id: "home", label: "Início", icon: Home }, { id: "explore", label: "Explorar", icon: Sparkles }, { id: "map", label: "Mapa vivo", icon: MapPinned }, { id: "reservas", label: "Reservas", icon: CalendarCheck2 }, { id: "community", label: "Comunidade", icon: UsersRound }, { id: "projects", label: "Projetos", icon: FolderKanban }, { id: "events", label: "Eventos", icon: CalendarDays }
];
const initialPosts: Post[] = [
  { id: 1, author: "Marina Santos", role: "Engenharia de Controle", initials: "MS", type: "Projeto", title: "IRIS está procurando alguém de front-end", body: "Nosso sistema de segurança por visão computacional ganhou uma nova fase. Estamos procurando uma pessoa para cuidar da experiência do dashboard.", tags: ["IA", "Front-end", "Visão computacional"], likes: 18, comments: 6, time: "há 24 min" },
  { id: 2, author: "Rafael Lima", role: "Mecatrônica", initials: "RL", type: "Descoberta", title: "A bancada 04 do Lab de Prototipagem está livre", body: "Para quem está precisando de uma bancada antiestática hoje: a 04 está com todos os instrumentos calibrados e disponível até às 18h.", tags: ["Prototipagem", "Dica rápida"], likes: 31, comments: 3, time: "há 1 h" },
  { id: 3, author: "Núcleo de Inovação", role: "CIMATEC", initials: "NI", type: "Evento", title: "Inscrições abertas para o Hackathon CIMATEC", body: "48 horas para construir soluções que aproximam indústria, pesquisa e impacto. Monte seu time e venha para o Hub.", tags: ["Hackathon", "Oportunidade"], likes: 42, comments: 12, time: "ontem" }
];
const events = [
  { title: "Hackathon CIMATEC 2026", category: "Competição", date: "26 — 28 set", place: "Hub de Inovação · CIMATEC 4", desc: "48 horas para transformar desafios reais da indústria em protótipos de impacto.", color: "#175cd3", attendees: 84 },
  { title: "Workshop: Visão computacional na prática", category: "Workshop", date: "21 set · 18:30", place: "Laboratório de IA & Visão", desc: "Do dataset ao primeiro modelo rodando em uma câmera industrial.", color: "#7c3aed", attendees: 32 },
  { title: "Demo Day de Projetos", category: "Apresentação", date: "02 out · 14:00", place: "Auditório Principal", desc: "Conheça os projetos que estão movimentando os laboratórios do CIMATEC.", color: "#0891b2", attendees: 61 }
];
const projects = [
  { name: "IRIS", desc: "Sistema de segurança utilizando visão computacional para ambientes industriais.", tech: ["Python", "OpenCV", "React"], lab: "Laboratório de IA & Visão", status: "Em desenvolvimento", team: ["MS", "RL", "AC"], need: "Front-end · UX" },
  { name: "EcoTrack", desc: "Monitoramento inteligente de consumo energético em pequenos laboratórios.", tech: ["Arduino", "IoT", "Dashboard"], lab: "Laboratório de Prototipagem", status: "Piloto", team: ["GB", "LA"], need: "Dados · Eletrônica" },
  { name: "Navi", desc: "Robô autônomo para orientação e acessibilidade nos prédios do campus.", tech: ["ROS", "LIDAR", "C++"], lab: "Arena de Robótica", status: "Exploração", team: ["CP", "JM", "VT"], need: "Robótica · IA" }
];

function getStored<T>(key: string, fallback: T): T { try { const value = localStorage.getItem(key); return value ? JSON.parse(value) : fallback; } catch { return fallback; } }
function saveStored(key: string, value: unknown) { localStorage.setItem(key, JSON.stringify(value)); }
function statusLabel(status: LabStatus) { return { available: "Disponível", busy: "Ocupado", reserved: "Reservado", maintenance: "Manutenção" }[status]; }
function statusClass(status: LabStatus) { return `status-dot ${status}`; }
function initials(name: string) { return name.split(" ").map((part) => part[0]).slice(0, 2).join("").toUpperCase(); }

function Logo({ compact = false }: { compact?: boolean }) {
  return <div className={`brand ${compact ? "compact" : ""}`} aria-label="LabMatch">
    <div className="brand-mark"><span></span><i></i><b></b></div>
    {!compact && <div><strong>Lab<span>Match</span></strong><small>laboratórios que combinam</small></div>}
  </div>;
}

function Splash({ onDone }: { onDone: () => void }) {
  useEffect(() => { const timer = setTimeout(() => { sessionStorage.setItem("labmatch_intro_seen", "1"); onDone(); }, 2200); return () => clearTimeout(timer); }, [onDone]);
  return <div className="splash"><div className="splash-grid"></div><div className="splash-orbit orbit-a"></div><div className="splash-orbit orbit-b"></div><div className="splash-core"><div className="splash-symbol"><span></span><i></i><b></b></div><div className="splash-word">Lab<span>Match</span></div><p>Encontra o laboratório que combina com sua atividade.</p></div><div className="splash-line"></div></div>;
}

function Login({ onLogin }: { onLogin: () => void }) {
  const [ra, setRa] = useState("20260001"); const [password, setPassword] = useState("123456"); const [remember, setRemember] = useState(true); const [loading, setLoading] = useState(false); const [error, setError] = useState("");
  function submit(e: React.FormEvent) { e.preventDefault(); setError(""); if (ra !== "20260001" || password !== "123456") { setError("Confira seu RA e senha de demonstração."); return; } setLoading(true); setTimeout(() => { saveStored("labmatch_logged", true); if (remember) saveStored("labmatch_remember", true); onLogin(); }, 900); }
  return <main className="login-page"><div className="login-panel"><div className="login-visual"><div className="login-glow"></div><div className="login-top"><Logo compact /><span className="pill-dark"><ShieldCheck size={14}/> Ambiente acadêmico seguro</span></div><div className="login-visual-copy"><p className="eyebrow light">SENAI CIMATEC · LABMATCH</p><h1>Ideias melhores<br/><em>começam no lugar certo.</em></h1><p>Descubra espaços, equipamentos e pessoas para transformar sua próxima atividade em algo concreto.</p><div className="login-proof"><div className="proof-avatars"><span>MS</span><span>RL</span><span>AC</span><b>+2k</b></div><div><strong>2.400+ estudantes</strong><small>encontram seu match todos os meses</small></div></div></div><div className="login-footer">Produto experimental de inovação · CIMATEC 2026</div></div><div className="login-form-wrap"><div className="login-form-head"><Logo /><span className="cimatec-tag">SENAI<br/><b>CIMATEC</b></span></div><div className="login-form-copy"><p className="eyebrow">BEM-VINDO DE VOLTA</p><h2>Entre no seu espaço.</h2><p>Use suas credenciais acadêmicas para continuar.</p></div><form onSubmit={submit} className="login-form"><label>RA<input value={ra} onChange={(e) => setRa(e.target.value)} inputMode="numeric" aria-label="RA" /></label><label>Senha<div className="password-field"><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} aria-label="Senha" /><LockKeyhole size={16}/></div></label>{error && <div className="form-error"><AlertTriangle size={15}/>{error}</div>}<div className="form-row"><label className="check-label"><input type="checkbox" checked={remember} onChange={(e) => setRemember(e.target.checked)}/><span></span> Lembrar acesso</label><button type="button" className="link-button" onClick={() => toast.info("Procure a secretaria acadêmica para redefinir seu acesso.")}>Recuperar senha</button></div><button className="primary-btn login-btn" disabled={loading}>{loading ? <><Loader2 size={17} className="spin"/> Validando acesso...</> : <>Entrar <ArrowRight size={17}/></>}</button></form><div className="demo-hint"><Sparkles size={15}/><span><b>Acesso demonstrativo</b> · RA 20260001 · senha 123456</span></div></div></div></main>;
}

function Sidebar({ page, setPage, onLogout, collapsed, setCollapsed }: { page: Page; setPage: (p: Page) => void; onLogout: () => void; collapsed: boolean; setCollapsed: (v: boolean) => void }) {
  return <aside className={`sidebar ${collapsed ? "collapsed" : ""}`}><div className="sidebar-head"><Logo compact={collapsed}/>{!collapsed && <button className="collapse-btn" onClick={() => setCollapsed(true)} aria-label="Recolher menu"><ChevronRight size={16}/></button>}</div><div className="sidebar-label">Workspace</div><nav>{navItems.map(({ id, label, icon: Icon }) => <button key={id} className={page === id ? "active" : ""} onClick={() => setPage(id)} title={collapsed ? label : undefined}><Icon size={18}/><span>{label}</span>{id === "reservas" && <em>1</em>}</button>)}</nav><div className="sidebar-label secondary-label">Sua conta</div><nav><button className={page === "notifications" ? "active" : ""} onClick={() => setPage("notifications")}><Bell size={18}/><span>Notificações</span><em className="notification-count">3</em></button><button className={page === "profile" ? "active" : ""} onClick={() => setPage("profile")}><UserRound size={18}/><span>Meu perfil</span></button></nav><div className="sidebar-bottom"><button onClick={() => setPage("preferences")} className={page === "preferences" ? "active" : ""}><Settings2 size={18}/><span>Preferências</span></button>{!collapsed && <div className="mini-profile"><div className="avatar">LM</div><div><strong>Lucca Martins</strong><small>Engenharia Mecatrônica</small></div><button onClick={onLogout} aria-label="Sair"><LogOut size={15}/></button></div>}{collapsed && <button className="expand-btn" onClick={() => setCollapsed(false)} aria-label="Expandir menu"><Menu size={18}/></button>}</div></aside>;
}

function Topbar({ page, setPage, theme, setTheme }: { page: Page; setPage: (p: Page) => void; theme: "light" | "dark"; setTheme: (t: "light" | "dark") => void }) {
  const titles: Record<Page, [string, string]> = { home: ["Visão geral", "Seu workspace hoje"], explore: ["Explorar laboratórios", "Encontre o espaço ideal para sua atividade"], map: ["Mapa vivo", "Uma visão demonstrativa dos espaços CIMATEC"], reservas: ["Minhas reservas", "Controle seus próximos momentos no laboratório"], community: ["Comunidade", "Ideias que circulam pelo CIMATEC"], projects: ["Projetos", "Encontre pessoas para construir com você"], events: ["Eventos & oportunidades", "O que está acontecendo no campus"], notifications: ["Central de notificações", "Tudo o que merece sua atenção"], profile: ["Meu perfil", "Sua jornada de inovação no CIMATEC"], preferences: ["Preferências", "Ajuste sua experiência no LabMatch"] };
  return <header className="topbar"><div className="mobile-brand"><Logo compact/></div><div className="topbar-heading"><p>{titles[page][1]}</p><h1>{titles[page][0]}</h1></div><div className="topbar-actions"><button className="top-search" onClick={() => setPage("explore")}><Search size={17}/><span>Buscar no LabMatch</span><kbd>⌘ K</kbd></button><button className="icon-btn" onClick={() => setTheme(theme === "light" ? "dark" : "light")} aria-label="Alternar tema">{theme === "light" ? <Moon size={18}/> : <Sun size={18}/>}</button><button className="icon-btn notification-button" onClick={() => setPage("notifications")} aria-label="Notificações"><Bell size={18}/><i></i></button><button className="top-avatar" onClick={() => setPage("profile")}>LM</button></div></header>;
}

function StatusBadge({ status }: { status: LabStatus }) { return <span className={`status-badge ${status}`}><i className={statusClass(status)}></i>{statusLabel(status)}</span>; }
function MatchRing({ score, small = false }: { score: number; small?: boolean }) { return <div className={`match-ring ${small ? "small" : ""}`} style={{ "--score": `${score * 3.6}deg` } as React.CSSProperties}><div><strong>{score}%</strong>{!small && <small>match</small>}</div></div>; }
function LabCard({ lab, onOpen, onBook }: { lab: Lab; onOpen: (lab: Lab) => void; onBook: (lab: Lab) => void }) {
  return <article className="lab-card"><div className="lab-card-top"><div className="lab-accent" style={{ background: lab.color }}></div><div><span className="category-label">{lab.category}</span><h3>{lab.name}</h3><p><MapPin size={13}/>{lab.building} · {lab.floor.split(" · ")[0]}</p></div><MatchRing score={lab.match} small/></div><div className="lab-card-mid"><StatusBadge status={lab.status}/><span className="capacity"><UsersRound size={14}/>{lab.occupancy}/{lab.capacity} pessoas</span></div><div className="lab-tags">{lab.equipment.slice(0, 3).map((item) => <span key={item}>{item}</span>)}{lab.equipment.length > 3 && <span>+{lab.equipment.length - 3}</span>}</div><div className="lab-card-actions"><button className="text-btn" onClick={() => onOpen(lab)}>Ver detalhes <ArrowUpRight size={15}/></button><button className="small-primary" disabled={lab.status === "maintenance"} onClick={() => onBook(lab)}>{lab.status === "maintenance" ? "Indisponível" : "Reservar"}</button></div></article>;
}

function HomePage({ reservations, onOpenLab, onBook, setPage, onCancel, search, setSearch }: { reservations: Reservation[]; onOpenLab: (lab: Lab) => void; onBook: (lab: Lab) => void; setPage: (p: Page) => void; onCancel: (id: string) => void; search: string; setSearch: (s: string) => void }) {
  const reservation = reservations.find((item) => item.status === "confirmed");
  const reservationLab = reservation ? labs.find((lab) => lab.id === reservation.labId) : null;
  return <div className="page home-page"><section className="hero-card"><div className="hero-pattern"></div><div className="hero-copy"><div className="eyebrow light">QUARTA-FEIRA · 16 SET 2026</div><h2>Olá, Lucca<span>.</span></h2><p>Encontre o espaço ideal para transformar sua próxima ideia em realidade.</p></div><div className="hero-search"><div className="search-leading"><Sparkles size={18}/></div><input value={search} onChange={(e) => setSearch(e.target.value)} onKeyDown={(e) => e.key === "Enter" && setPage("explore")} placeholder="Ex.: montar um protótipo com Arduino para 4 pessoas" /><button onClick={() => setPage("explore")}>Encontrar laboratório <ArrowRight size={16}/></button></div><div className="suggestion-row"><span>Comece por</span><button onClick={() => { setSearch("Arduino"); setPage("explore"); }}>Arduino</button><button onClick={() => { setSearch("impressão 3D"); setPage("explore"); }}>Impressão 3D</button><button onClick={() => { setSearch("espaço para equipe"); setPage("explore"); }}>Espaço para equipe</button></div></section><section className="stat-row"><div><span className="stat-icon blue"><Activity size={17}/></span><div><strong>07</strong><span>labs disponíveis agora</span></div></div><div><span className="stat-icon green"><CalendarCheck2 size={17}/></span><div><strong>{reservations.filter((r) => r.status === "confirmed").length}</strong><span>reserva próxima</span></div></div><div><span className="stat-icon purple"><UsersRound size={17}/></span><div><strong>32</strong><span>pessoas construindo hoje</span></div></div><div><span className="stat-icon orange"><Zap size={17}/></span><div><strong>94%</strong><span>seu último match</span></div></div></section><div className="content-grid home-grid"><section className="main-column"><div className="section-heading"><div><p className="eyebrow">AGENDA</p><h2>Próxima reserva</h2></div><button className="text-btn" onClick={() => setPage("reservas")}>Ver todas <ArrowRight size={15}/></button></div>{reservation && reservationLab ? <div className="reservation-card"><div className="reservation-date"><strong>18</strong><span>SET</span></div><div className="reservation-info"><div className="reservation-title"><div><span className="category-label">RESERVA CONFIRMADA</span><h3>{reservationLab.name}</h3></div><span className="confirmed"><CheckCircle2 size={14}/> Confirmada</span></div><p><CalendarDays size={15}/> {reservation.date} · {reservation.time} <span>·</span> <Clock3 size={15}/> {reservation.duration}</p><p><Lightbulb size={15}/> {reservation.objective}</p><div className="reservation-footer"><span><MapPin size={14}/>{reservationLab.building} · {reservationLab.floor}</span><span><UsersRound size={14}/>{reservation.people} pessoas</span><div><button className="outline-btn" onClick={() => setPage("reservas")}>Abrir reserva</button><button className="danger-ghost" onClick={() => onCancel(reservation.id)}><Trash2 size={14}/> Cancelar</button></div></div></div></div> : <div className="empty-card"><div className="empty-icon"><CalendarDays size={22}/></div><h3>Você não possui reservas próximas.</h3><p>Encontre um laboratório e reserve um momento para construir sua próxima ideia.</p><button className="primary-btn" onClick={() => setPage("explore")}>Encontrar laboratório <ArrowRight size={16}/></button></div>}<div className="section-heading recommendation-heading"><div><p className="eyebrow">PARA VOCÊ</p><h2>Laboratórios recomendados</h2></div><button className="text-btn" onClick={() => setPage("explore")}>Explorar todos <ArrowRight size={15}/></button></div><div className="lab-list compact-list">{labs.slice(0, 3).map((lab) => <LabCard key={lab.id} lab={lab} onOpen={onOpenLab} onBook={onBook}/>)}</div></section><aside className="side-column"><div className="panel now-panel"><div className="panel-heading"><div><p className="eyebrow">LABMATCH AGORA</p><h3>O campus em movimento</h3></div><span className="live-indicator"><i></i> ao vivo</span></div><div className="now-summary"><div className="donut"><strong>68%</strong><span>ocupação</span></div><div><strong>18 <small>de 25</small></strong><span>laboratórios em uso</span><div className="mini-bar"><i style={{ width: "68%" }}></i></div></div></div><div className="now-list">{labs.slice(0, 4).map((lab) => <button key={lab.id} onClick={() => onOpenLab(lab)}><span className="now-name"><i className={statusClass(lab.status)}></i>{lab.name}</span><span>{lab.occupancy}/{lab.capacity}</span></button>)}</div><button className="panel-link" onClick={() => setPage("map")}>Ver mapa vivo <ArrowUpRight size={15}/></button></div><div className="panel pulse-panel"><div className="panel-heading"><div><p className="eyebrow">O QUE ESTÁ ACONTECENDO</p><h3>Pulso do campus</h3></div><MoreHorizontal size={18}/></div><div className="pulse-item"><span className="pulse-icon blue"><BookOpen size={16}/></span><div><strong>Workshop de IA</strong><span>Laboratório de IA · agora</span></div><ArrowUpRight size={15}/></div><div className="pulse-item"><span className="pulse-icon orange"><Wrench size={16}/></span><div><strong>Equipe no protótipo</strong><span>Arena de Robótica · há 12 min</span></div><ArrowUpRight size={15}/></div><div className="pulse-item"><span className="pulse-icon purple"><Lightbulb size={16}/></span><div><strong>Projeto EcoTrack</strong><span>Lab de Prototipagem · há 28 min</span></div><ArrowUpRight size={15}/></div><button className="panel-link" onClick={() => setPage("community")}>Ir para comunidade <ArrowUpRight size={15}/></button></div></aside></div></div>;
}

function ExplorePage({ onOpenLab, onBook, search, setSearch }: { onOpenLab: (lab: Lab) => void; onBook: (lab: Lab) => void; search: string; setSearch: (s: string) => void }) {
  const [building, setBuilding] = useState("Todos os prédios"); const [status, setStatus] = useState("Todos"); const [equipment, setEquipment] = useState("Todos"); const [filtersOpen, setFiltersOpen] = useState(false);
  const filtered = useMemo(() => labs.filter((lab) => { const match = !search || [lab.name, lab.category, lab.building, ...lab.equipment, ...lab.resources].join(" ").toLowerCase().includes(search.toLowerCase()); const b = building === "Todos os prédios" || lab.building === building; const s = status === "Todos" || statusLabel(lab.status) === status; const e = equipment === "Todos" || lab.equipment.includes(equipment); return match && b && s && e; }), [search, building, status, equipment]);
  const equipmentOptions = ["Todos", "Arduino", "Raspberry Pi", "Impressora 3D", "Eletrônica", "Robótica"];
  return <div className="page"><div className="explore-intro"><div><p className="eyebrow">BUSCA INTELIGENTE</p><h2>Qual é o próximo passo?</h2><p>Combine atividade, equipamento e disponibilidade. O LabMatch faz o resto.</p></div><div className="explore-actions"><button className="outline-btn" onClick={() => setFiltersOpen(!filtersOpen)}><SlidersHorizontal size={16}/> Filtros <span className="filter-count">{[building !== "Todos os prédios", status !== "Todos", equipment !== "Todos"].filter(Boolean).length || ""}</span></button><button className="primary-btn" onClick={() => toast.success("Preferência salva para suas próximas buscas.")}><Bookmark size={16}/> Salvar busca</button></div></div><div className="explore-search"><Search size={18}/><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Busque por laboratório, equipamento ou atividade..." /><kbd>⌘ K</kbd></div><div className={`filter-bar ${filtersOpen ? "open" : ""}`}><label>Prédio<select value={building} onChange={(e) => setBuilding(e.target.value)}>{buildings.map((b) => <option key={b}>{b}</option>)}</select></label><label>Status<select value={status} onChange={(e) => setStatus(e.target.value)}>{["Todos", "Disponível", "Ocupado", "Reservado", "Manutenção"].map((s) => <option key={s}>{s}</option>)}</select></label><label>Equipamento<select value={equipment} onChange={(e) => setEquipment(e.target.value)}>{equipmentOptions.map((e) => <option key={e}>{e}</option>)}</select></label><button className="reset-btn" onClick={() => { setBuilding("Todos os prédios"); setStatus("Todos"); setEquipment("Todos"); setSearch(""); }}>Limpar filtros</button></div><div className="results-meta"><span><strong>{filtered.length}</strong> laboratórios encontrados</span><button className="sort-button">Maior compatibilidade <ChevronDown size={15}/></button></div>{filtered.length ? <div className="lab-grid">{filtered.map((lab) => <LabCard key={lab.id} lab={lab} onOpen={onOpenLab} onBook={onBook}/>)}</div> : <div className="empty-card explore-empty"><div className="empty-icon"><Search size={22}/></div><h3>Nenhum laboratório combina com esses filtros.</h3><p>Tente buscar por outro equipamento ou ampliar a disponibilidade.</p><button className="outline-btn" onClick={() => { setBuilding("Todos os prédios"); setStatus("Todos"); setEquipment("Todos"); setSearch(""); }}>Limpar busca</button></div>}</div>;
}

function MapPage({ onOpenLab, setPage }: { onOpenLab: (lab: Lab) => void; setPage: (p: Page) => void }) {
  const [selectedBuilding, setSelectedBuilding] = useState("CIMATEC 2"); const [mapStatus, setMapStatus] = useState<LabStatus | "all">("all"); const [zoom, setZoom] = useState(1);
  const buildingLabs = labs.filter((lab) => lab.building === selectedBuilding && (mapStatus === "all" || lab.status === mapStatus));
  return <div className="page map-page"><div className="map-toolbar"><div><p className="eyebrow">REPRESENTAÇÃO DEMONSTRATIVA</p><h2>Mapa vivo do CIMATEC</h2><p className="muted">Explore prédios e laboratórios por disponibilidade.</p></div><button className="outline-btn" onClick={() => { toast.success("Localização simulada: você está próximo ao CIMATEC 2."); setSelectedBuilding("CIMATEC 2"); }}><LocateFixed size={16}/> Onde estou?</button></div><div className="map-layout"><section className="map-canvas"><div className="map-canvas-top"><span className="map-note"><Circle size={8} fill="currentColor"/> atualização simulada · 30s</span><div className="zoom-controls"><button onClick={() => setZoom(Math.min(1.2, zoom + .1))}><ZoomIn size={16}/></button><span>{Math.round(zoom * 100)}%</span><button onClick={() => setZoom(Math.max(.8, zoom - .1))}><ZoomOut size={16}/></button></div></div><div className="campus-map" style={{ transform: `scale(${zoom})` }}><div className="map-road road-a"></div><div className="map-road road-b"></div><div className="map-green green-a"></div><div className="map-green green-b"></div>{["CIMATEC 1", "CIMATEC 2", "CIMATEC 3", "CIMATEC 4"].map((building, i) => <button key={building} className={`map-building building-${i + 1} ${selectedBuilding === building ? "selected" : ""}`} onClick={() => setSelectedBuilding(building)}><Building2 size={22}/><strong>{building}</strong><span>{labs.filter((lab) => lab.building === building).length} labs</span></button>)}<div className="map-pin"><LocateFixed size={14}/></div></div><div className="map-legend"><span><i className={statusClass("available")}></i>Disponível</span><span><i className={statusClass("busy")}></i>Ocupado</span><span><i className={statusClass("reserved")}></i>Reservado</span><span><i className={statusClass("maintenance")}></i>Manutenção</span></div></section><aside className="map-side"><div className="map-side-head"><div><p className="eyebrow">PRÉDIO SELECIONADO</p><h3>{selectedBuilding}</h3></div><Layers3 size={19}/></div><div className="map-filter-tabs"><button className={mapStatus === "all" ? "active" : ""} onClick={() => setMapStatus("all")}>Todos</button>{(["available", "busy", "reserved"] as LabStatus[]).map((s) => <button key={s} className={mapStatus === s ? "active" : ""} onClick={() => setMapStatus(s)}><i className={statusClass(s)}></i></button>)}</div><div className="nearby-label">LABORATÓRIOS NESTE PRÉDIO <span>{buildingLabs.length}</span></div>{buildingLabs.map((lab) => <button className="nearby-lab" key={lab.id} onClick={() => onOpenLab(lab)}><span className="nearby-icon" style={{ background: `${lab.color}16`, color: lab.color }}><Building2 size={17}/></span><div><strong>{lab.name}</strong><span>{lab.floor}</span></div><div className="nearby-status"><i className={statusClass(lab.status)}></i><small>{lab.occupancy}/{lab.capacity}</small></div></button>)}<div className="where-card"><div className="where-icon"><Route size={18}/></div><div><strong>Você está próximo ao CIMATEC 2</strong><span>O laboratório disponível mais perto fica a 2 min.</span></div><button onClick={() => setPage("explore")}><ArrowRight size={16}/></button></div></aside></div><p className="disclaimer"><AlertTriangle size={14}/> O mapa é uma representação demonstrativa para o protótipo e não representa a arquitetura oficial real do CIMATEC.</p></div>;
}

function DetailModal({ lab, onClose, onBook }: { lab: Lab; onClose: () => void; onBook: (lab: Lab) => void }) {
  return <div className="modal-backdrop" onMouseDown={onClose}><div className="modal large-modal" onMouseDown={(e) => e.stopPropagation()}><div className="modal-cover" style={{ background: `linear-gradient(135deg, ${lab.color}, #0b1c3d)` }}><div className="cover-grid"></div><button className="modal-close light-close" onClick={onClose}><X size={18}/></button><div className="modal-cover-content"><span className="category-label light-label">{lab.category}</span><h2>{lab.name}</h2><p><MapPin size={14}/>{lab.building} · {lab.floor}</p></div><MatchRing score={lab.match}/></div><div className="modal-body"><div className="detail-main"><div className="detail-intro"><StatusBadge status={lab.status}/><p>{lab.description}</p></div><div className="detail-stats"><div><UsersRound size={16}/><span>Capacidade</span><strong>Até {lab.capacity} pessoas</strong></div><div><Clock3 size={16}/><span>Funcionamento</span><strong>{lab.hours}</strong></div><div><Accessibility size={16}/><span>Acessibilidade</span><strong>{lab.accessibility.length} recursos</strong></div></div><div className="detail-columns"><div><h4>Equipamentos</h4><div className="detail-tags">{lab.equipment.map((item) => <span key={item}><Check size={13}/>{item}</span>)}</div></div><div><h4>Recursos do espaço</h4><div className="detail-tags">{lab.resources.map((item) => <span key={item}><Check size={13}/>{item}</span>)}</div></div></div><div className="detail-projects"><div><p className="eyebrow">PROJETOS DESENVOLVIDOS AQUI</p><h4>Conexões que acontecem neste espaço</h4></div>{lab.projects.map((project) => <span key={project}><FolderKanban size={14}/>{project}<ArrowUpRight size={13}/></span>)}</div></div><aside className="score-explainer"><p className="eyebrow">MATCH SCORE</p><h3>Por que {lab.match}%?</h3><p>Este espaço combina com o que você busca.</p>{["Capacidade adequada", "Equipamentos disponíveis", "Horário compatível", "Localização conveniente", "Acessibilidade"].map((item) => <span key={item}><CheckCircle2 size={15}/>{item}</span>)}{lab.id === 4 && <div className="score-alert"><AlertTriangle size={15}/> A impressora 3D fica disponível a partir das 16h.</div>}</aside></div><div className="modal-footer"><button className="outline-btn" onClick={onClose}>Voltar</button><button className="primary-btn" onClick={() => onBook(lab)} disabled={lab.status === "maintenance"}>{lab.status === "maintenance" ? "Em manutenção" : "Reservar este laboratório"} <ArrowRight size={16}/></button></div></div></div>;
}

function BookingModal({ lab, onClose, onBooked }: { lab: Lab; onClose: () => void; onBooked: (reservation: Reservation) => void }) {
  const [step, setStep] = useState(1); const [objective, setObjective] = useState("Montar um protótipo com Arduino"); const [date, setDate] = useState("18"); const [time, setTime] = useState("14:00"); const [people, setPeople] = useState(4); const [needs, setNeeds] = useState<string[]>(["Arduino"]); const [access, setAccess] = useState(false); const options = ["Arduino", "Raspberry Pi", "Impressora 3D", "Baixo ruído"];
  const compatible = people <= lab.capacity && lab.status !== "maintenance" && !(needs.includes("Impressora 3D") && lab.id === 1 && time === "14:00");
  function toggleNeed(item: string) { setNeeds((current) => current.includes(item) ? current.filter((i) => i !== item) : [...current, item]); }
  function submit() { if (!compatible) return; const reservation: Reservation = { id: `LM-${Math.floor(Math.random() * 8000 + 1000)}`, labId: lab.id, date: `${date} set 2026`, time: `${time} — ${time === "14:00" ? "16:00" : "12:00"}`, duration: "2 horas", objective, people, status: "confirmed" }; onBooked(reservation); }
  return <div className="modal-backdrop" onMouseDown={onClose}><div className="modal booking-modal" onMouseDown={(e) => e.stopPropagation()}><div className="booking-head"><div><p className="eyebrow">RESERVA INTELIGENTE</p><h2>{lab.name}</h2><span><MapPin size={14}/>{lab.building} · {lab.floor}</span></div><button className="modal-close" onClick={onClose}><X size={18}/></button></div><div className="stepper">{["Atividade", "Quando", "Pessoas", "Recursos", "Revisão"].map((label, i) => <div key={label} className={step > i ? "done" : step === i + 1 ? "current" : ""}><span>{step > i + 1 ? <Check size={13}/> : i + 1}</span><small>{label}</small></div>)}</div><div className="booking-content">{step === 1 && <div className="booking-step"><h3>O que você vai fazer?</h3><p>Conte brevemente para encontrarmos a melhor configuração.</p><label className="big-label">Objetivo da atividade<textarea value={objective} onChange={(e) => setObjective(e.target.value)} rows={3}/></label><div className="smart-suggestion"><Sparkles size={16}/><span><b>Match inicial de {lab.match}%</b><small>Baseado em sua atividade e neste laboratório.</small></span></div></div>}{step === 2 && <div className="booking-step"><h3>Quando você precisa do espaço?</h3><p>Escolha uma data e um horário que funcionem para sua equipe.</p><div className="field-row"><label className="big-label">Data<select value={date} onChange={(e) => setDate(e.target.value)}><option value="18">18 de setembro, 2026</option><option value="19">19 de setembro, 2026</option><option value="21">21 de setembro, 2026</option></select></label><label className="big-label">Horário<select value={time} onChange={(e) => setTime(e.target.value)}><option>14:00</option><option>16:00</option><option>18:00</option></select></label></div><div className="availability-note"><CheckCircle2 size={16}/> Horário disponível para este laboratório</div></div>}{step === 3 && <div className="booking-step"><h3>Quantas pessoas vão participar?</h3><p>Isso nos ajuda a evitar espaços apertados e manter a experiência segura.</p><div className="people-picker"><button onClick={() => setPeople(Math.max(1, people - 1))}>−</button><strong>{people}</strong><span>pessoas</span><button onClick={() => setPeople(Math.min(40, people + 1))}>+</button></div>{people > lab.capacity && <div className="prevention-error"><AlertTriangle size={17}/><div><strong>Este laboratório não comporta {people} pessoas.</strong><span>Capacidade máxima: {lab.capacity}. Sugerimos o Laboratório de Prototipagem 02 · 91% Match.</span></div></div>}<div className="capacity-bar"><span>Capacidade do espaço</span><div><i style={{ width: `${Math.min(100, people / lab.capacity * 100)}%` }}></i></div><small>{people}/{lab.capacity}</small></div></div>}{step === 4 && <div className="booking-step"><h3>O que você precisa?</h3><p>Selecione equipamentos e preferências para o seu momento.</p><div className="option-grid">{options.map((item) => <button key={item} className={needs.includes(item) ? "selected" : ""} onClick={() => toggleNeed(item)}><span>{item === "Arduino" ? <Cpu size={18}/> : item === "Raspberry Pi" ? <Laptop size={18}/> : item === "Impressora 3D" ? <Printer size={18}/> : <Volume2 size={18}/>}</span>{item}{needs.includes(item) && <CheckCircle2 size={15}/>}</button>)}</div><label className="switch-row"><button className={`switch ${access ? "on" : ""}`} onClick={() => setAccess(!access)}><i></i></button><span><strong>Preciso de recursos de acessibilidade</strong><small>Espaço para cadeira, mesa adaptada ou baixo ruído.</small></span></label></div>}{step === 5 && <div className="booking-step"><h3>Confira sua reserva</h3><p>Tudo certo? Você poderá cancelar até 30 min antes.</p><div className="review-card"><div><span>Laboratório</span><strong>{lab.name}</strong></div><div><span>Quando</span><strong>18 de setembro · {time} · 2 horas</strong></div><div><span>Atividade</span><strong>{objective}</strong></div><div><span>Equipe</span><strong>{people} pessoas · {needs.join(", ") || "configuração padrão"}</strong></div></div>{!compatible && <div className="prevention-error"><AlertTriangle size={17}/><div><strong>Não é possível confirmar esta reserva.</strong><span>Revise capacidade, horário ou disponibilidade do equipamento.</span></div></div>}</div>}</div><div className="booking-footer"><button className="outline-btn" onClick={() => step === 1 ? onClose() : setStep(step - 1)}>{step === 1 ? "Cancelar" : "Voltar"}</button><button className="primary-btn" disabled={(step === 3 && people > lab.capacity) || (step === 5 && !compatible)} onClick={() => step < 5 ? setStep(step + 1) : submit()}>{step < 5 ? <>Continuar <ArrowRight size={16}/></> : <>Confirmar reserva <Check size={16}/></>}</button></div></div></div>;
}

function ReservationsPage({ reservations, onCancel, onBook }: { reservations: Reservation[]; onCancel: (id: string) => void; onBook: () => void }) {
  const [tab, setTab] = useState("Próximas"); const upcoming = reservations.filter((r) => r.status === "confirmed"); const history = reservations.filter((r) => r.status !== "confirmed");
  return <div className="page"><div className="page-intro-row"><div><p className="eyebrow">SUA AGENDA</p><h2>Momentos para construir</h2><p className="muted">Reserve, acompanhe e faça check-in sem perder o ritmo.</p></div><button className="primary-btn" onClick={onBook}><Plus size={16}/> Nova reserva</button></div><div className="tabs">{["Próximas", "Histórico", "Lista de espera", "QR Check-in"].map((item) => <button key={item} className={tab === item ? "active" : ""} onClick={() => setTab(item)}>{item}{item === "Próximas" && upcoming.length > 0 && <span>{upcoming.length}</span>}</button>)}</div>{tab === "Próximas" && <div className="reservation-list">{upcoming.length ? upcoming.map((reservation) => { const lab = labs.find((l) => l.id === reservation.labId)!; return <div className="reservation-full-card" key={reservation.id}><div className="reservation-full-accent" style={{ background: lab.color }}></div><div className="reservation-full-body"><div className="reservation-title"><div><span className="category-label">{reservation.id} · RESERVA CONFIRMADA</span><h3>{lab.name}</h3></div><StatusBadge status={lab.status}/></div><div className="reservation-detail-grid"><span><CalendarDays size={15}/><b>Data & horário</b>{reservation.date} · {reservation.time}</span><span><Lightbulb size={15}/><b>Objetivo</b>{reservation.objective}</span><span><MapPin size={15}/><b>Localização</b>{lab.building} · {lab.floor}</span><span><UsersRound size={15}/><b>Equipe</b>{reservation.people} pessoas</span></div><div className="reservation-card-actions"><button className="outline-btn" onClick={() => toast.success("QR Check-in disponível 30 minutos antes do horário.")}><QrCode size={15}/> QR Check-in</button><button className="danger-ghost" onClick={() => onCancel(reservation.id)}><Trash2 size={14}/> Cancelar reserva</button></div></div></div>; }) : <div className="empty-card"><div className="empty-icon"><CalendarDays size={22}/></div><h3>Você ainda não possui reservas.</h3><p>Seu próximo projeto pode começar com uma bancada disponível.</p><button className="primary-btn" onClick={onBook}>Encontrar laboratório <ArrowRight size={16}/></button></div>}</div>}{tab === "Histórico" && <div className="history-list">{history.length ? history.map((r) => { const lab = labs.find((l) => l.id === r.labId)!; return <div className="history-row" key={r.id}><div className="history-icon"><CheckCircle2 size={17}/></div><div><strong>{lab.name}</strong><span>{r.date} · {r.objective}</span></div><span className={`history-status ${r.status}`}>{r.status === "cancelled" ? "Cancelada" : "Check-in realizado"}</span></div>; }) : <div className="empty-card"><div className="empty-icon"><Clock3 size={22}/></div><h3>Seu histórico aparecerá aqui.</h3><p>Depois de usar um espaço, você encontrará seus registros nesta aba.</p></div>}</div>}{tab === "Lista de espera" && <div className="wait-card"><div className="wait-visual"><TicketCheck size={26}/></div><div><p className="eyebrow">LISTA DE ESPERA ATIVA</p><h3>Oficina de Materiais</h3><p>Você está em <strong>#3</strong> e receberá uma notificação quando uma vaga for liberada.</p><div className="wait-progress"><i></i><i></i><i className="current"></i><i></i><i></i></div><button className="danger-ghost" onClick={() => toast.success("Você saiu da lista de espera.")}>Sair da lista</button></div></div>}{tab === "QR Check-in" && <div className="qr-card"><div className="qr-code">{Array.from({ length: 81 }).map((_, i) => <i key={i} className={(i * 7 + i * i) % 5 < 2 ? "on" : ""}></i>)}</div><div><p className="eyebrow">PRÓXIMO CHECK-IN</p><h3>Laboratório de Prototipagem</h3><p>18 set 2026 · 14:00 — 16:00</p><span className="qr-valid"><CheckCircle2 size={14}/> QR válido a partir de 13:30</span><button className="primary-btn" onClick={() => toast.success("Check-in realizado. Bom trabalho, Lucca!")}><TicketCheck size={16}/> Simular check-in</button></div></div>}</div>;
}

function CommunityPage({ posts, setPosts }: { posts: Post[]; setPosts: React.Dispatch<React.SetStateAction<Post[]>> }) {
  const [compose, setCompose] = useState(false); const [title, setTitle] = useState(""); const [body, setBody] = useState(""); const [type, setType] = useState("Projeto");
  function publish() { if (!title.trim() || !body.trim()) { toast.error("Adicione um título e uma descrição para publicar."); return; } const post: Post = { id: Date.now(), author: "Lucca Martins", role: "Engenharia Mecatrônica", initials: "LM", type, title, body, tags: ["Nova publicação"], likes: 0, comments: 0, time: "agora" }; const next = [post, ...posts]; setPosts(next); saveStored("labmatch_posts", next); setTitle(""); setBody(""); setCompose(false); toast.success("Publicação compartilhada com a comunidade."); }
  function updatePost(id: number, key: "liked" | "saved") { const next = posts.map((post) => post.id === id ? { ...post, [key]: !post[key], likes: key === "liked" ? post.likes + (post.liked ? -1 : 1) : post.likes } : post); setPosts(next); saveStored("labmatch_posts", next); }
  return <div className="page community-page"><div className="community-head"><div><p className="eyebrow">LABMATCH COMMUNITY</p><h2>O campus pensa junto.</h2><p className="muted">Compartilhe descobertas, encontre parceiros e acompanhe o que está sendo construído.</p></div><button className="primary-btn" onClick={() => setCompose(!compose)}><Plus size={16}/> Criar publicação</button></div>{compose && <div className="compose-card"><div className="compose-head"><div className="avatar">LM</div><div><strong>Compartilhe com a comunidade</strong><span>O que está acontecendo?</span></div><button className="icon-btn" onClick={() => setCompose(false)}><X size={17}/></button></div><div className="compose-tabs">{["Projeto", "Pergunta", "Oportunidade", "Evento", "Equipe"].map((item) => <button key={item} className={type === item ? "active" : ""} onClick={() => setType(item)}>{item}</button>)}</div><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Dê um título para sua publicação" /><textarea value={body} onChange={(e) => setBody(e.target.value)} placeholder="Conte mais sobre sua ideia, descoberta ou oportunidade..." rows={4}/><div className="compose-footer"><span><MapPin size={14}/> Adicionar laboratório</span><button className="primary-btn" onClick={publish}>Publicar <Send size={15}/></button></div></div>}<div className="community-layout"><div className="feed-column"><div className="feed-filters"><button className="active">Para você</button><button>Mais recentes</button><button>Projetos</button><button>Oportunidades</button><SlidersHorizontal size={16}/></div>{posts.map((post) => <article className="post-card" key={post.id}><div className="post-head"><div className={`avatar ${post.id === 3 ? "institutional" : ""}`}>{post.initials}</div><div><strong>{post.author}</strong><span>{post.role} · {post.time}</span></div><button className="icon-btn"><MoreHorizontal size={17}/></button></div><div className="post-type"><span>{post.type}</span></div><h3>{post.title}</h3><p>{post.body}</p><div className="post-tags">{post.tags.map((tag) => <span key={tag}>#{tag.replaceAll(" ", "")}</span>)}</div><div className="post-actions"><button className={post.liked ? "selected" : ""} onClick={() => updatePost(post.id, "liked")}><Heart size={16} fill={post.liked ? "currentColor" : "none"}/> {post.likes}</button><button><MessageCircle size={16}/> {post.comments}</button><button><Share2 size={16}/> Compartilhar</button><button className={post.saved ? "selected" : ""} onClick={() => updatePost(post.id, "saved")}><Bookmark size={16} fill={post.saved ? "currentColor" : "none"}/></button></div></article>)}</div><aside className="community-side"><div className="panel community-widget"><div className="panel-heading"><div><p className="eyebrow">ENCONTRE SUA EQUIPE</p><h3>Projetos procurando você</h3></div><ArrowUpRight size={16}/></div>{projects.slice(0, 2).map((project) => <div className="team-project" key={project.name}><div className="project-avatar" style={{ background: project.name === "IRIS" ? "#eef4ff" : "#ecfdf5", color: project.name === "IRIS" ? "#175cd3" : "#059669" }}>{project.name.slice(0, 1)}</div><div><strong>{project.name}</strong><span>Procura: {project.need}</span></div><button onClick={() => toast.success("Interesse enviado para a equipe.")}><UserPlus size={15}/></button></div>)}<button className="panel-link">Ver todas as equipes <ArrowRight size={15}/></button></div><div className="panel trend-widget"><p className="eyebrow">EM ALTA NO CIMATEC</p><div className="trend-item"><span>01</span><strong>visão computacional</strong><small>128 conversas</small></div><div className="trend-item"><span>02</span><strong>prototipagem rápida</strong><small>94 conversas</small></div><div className="trend-item"><span>03</span><strong>robótica autônoma</strong><small>72 conversas</small></div></div></aside></div></div>;
}

function ProjectsPage({ setPage }: { setPage: (p: Page) => void }) { return <div className="page"><div className="page-intro-row"><div><p className="eyebrow">CONSTRUINDO JUNTOS</p><h2>Projetos em movimento</h2><p className="muted">Veja onde as ideias do CIMATEC estão ganhando forma.</p></div><button className="primary-btn" onClick={() => setPage("community")}><Plus size={16}/> Criar projeto</button></div><div className="project-highlight"><div><span className="pill-dark">DESTAQUE DA SEMANA</span><h2>Da bancada para o mundo.</h2><p>Projetos que começam em uma ideia, encontram o laboratório certo e chegam mais longe com as pessoas certas.</p><button className="light-btn" onClick={() => setPage("community")}>Conhecer a comunidade <ArrowRight size={16}/></button></div><div className="highlight-orbit"><div><FolderKanban size={29}/><span>26</span><small>projetos ativos</small></div><div><UsersRound size={29}/><span>84</span><small>estudantes criando</small></div></div></div><div className="project-filter-row"><div className="segmented"><button className="active">Todos</button><button>Em desenvolvimento</button><button>Piloto</button><button>Exploração</button></div><button className="outline-btn"><SlidersHorizontal size={15}/> Filtrar</button></div><div className="project-grid">{projects.map((project, i) => <article className="project-card" key={project.name}><div className={`project-cover cover-${i + 1}`}><span>{project.status}</span><strong>{project.name}</strong><div className="project-symbol"><FolderKanban size={26}/></div></div><div className="project-card-body"><p>{project.desc}</p><div className="project-tech">{project.tech.map((tech) => <span key={tech}>{tech}</span>)}</div><div className="project-meta"><span><MapPin size={14}/>{project.lab}</span><div className="stacked-avatars">{project.team.map((member) => <i key={member}>{member}</i>)}</div></div><div className="project-need"><span>Procurando</span><strong>{project.need}</strong><button onClick={() => toast.success(`Você demonstrou interesse no projeto ${project.name}.`)}>Tenho interesse <ArrowUpRight size={14}/></button></div></div></article>)}</div></div>; }

function EventsPage() { const [filter, setFilter] = useState("Todos"); return <div className="page"><div className="page-intro-row"><div><p className="eyebrow">AGENDA CIMATEC</p><h2>Acontece por aqui.</h2><p className="muted">Workshops, hackathons e oportunidades para você continuar avançando.</p></div><button className="outline-btn" onClick={() => toast.success("Você receberá lembretes dos próximos eventos.")}><Bell size={15}/> Ativar lembretes</button></div><div className="event-filters">{["Todos", "Workshops", "Hackathons", "Palestras", "Oportunidades"].map((item) => <button key={item} className={filter === item ? "active" : ""} onClick={() => setFilter(item)}>{item}</button>)}</div><div className="event-grid">{events.map((event) => <article className="event-card" key={event.title}><div className="event-cover" style={{ background: `linear-gradient(135deg, ${event.color}, #10254b)` }}><div className="event-cover-grid"></div><span>{event.category}</span><strong>{event.date}</strong><div className="event-mark"><Zap size={24}/></div></div><div className="event-body"><h3>{event.title}</h3><p>{event.desc}</p><span className="event-place"><MapPin size={14}/>{event.place}</span><div className="event-footer"><span><UsersRound size={14}/>{event.attendees} interessados</span><button className="small-primary" onClick={() => toast.success("Evento salvo na sua agenda.")}><Bookmark size={14}/> Quero participar</button></div></div></article>)}</div></div>; }

function NotificationsPage() { const [read, setRead] = useState(false); const items = [{ icon: CalendarCheck2, title: "Seu laboratório estará disponível em 30 minutos.", desc: "Laboratório de Prototipagem · hoje, 13:30", type: "Reserva", color: "blue" }, { icon: TicketCheck, title: "Uma vaga foi liberada na sua lista de espera.", desc: "Oficina de Materiais · hoje, 10:02", type: "Reserva", color: "green" }, { icon: MessageCircle, title: "João comentou no seu projeto.", desc: "IRIS · ontem, 18:40", type: "Comunidade", color: "purple" }, { icon: Zap, title: "Novo hackathon disponível.", desc: "Hackathon CIMATEC 2026 · ontem, 14:12", type: "Evento", color: "orange" }]; return <div className="page"><div className="page-intro-row"><div><p className="eyebrow">CENTRAL INTELIGENTE</p><h2>O que você perdeu?</h2><p className="muted">Atualizações importantes sobre seus espaços, projetos e comunidade.</p></div><button className="outline-btn" onClick={() => { setRead(true); toast.success("Todas as notificações foram marcadas como lidas."); }}><Check size={15}/> Marcar todas como lidas</button></div><div className="notification-list">{items.map((item, i) => { const Icon = item.icon; return <div className={`notification-row ${read || i > 1 ? "read" : ""}`} key={item.title}><div className={`notification-icon ${item.color}`}><Icon size={17}/></div><div className="notification-content"><div><strong>{item.title}</strong>{!read && i < 2 && <i className="unread-dot"></i>}</div><span>{item.desc}</span></div><span className="notification-type">{item.type}</span><MoreHorizontal size={17} className="notification-more"/></div>})}</div><div className="notification-footer"><CheckCircle2 size={18}/><div><strong>Você está em dia.</strong><span>Não há mais notificações pendentes por enquanto.</span></div></div></div>; }

function ProfilePage({ setPage }: { setPage: (p: Page) => void }) { return <div className="page profile-page"><div className="profile-hero"><div className="profile-avatar-large">LM<span><BadgeCheck size={16}/></span></div><div><p className="eyebrow light">PERFIL DO ESTUDANTE</p><h2>Lucca Martins</h2><p>Engenharia Mecatrônica · RA 20260001</p><div className="profile-tags"><span>Prototipagem</span><span>Robótica</span><span>Experiência do usuário</span></div></div><button className="light-btn" onClick={() => setPage("preferences")}><Settings2 size={15}/> Editar perfil</button></div><div className="profile-stats"><div><strong>12</strong><span>laboratórios utilizados</span></div><div><strong>04</strong><span>projetos em colaboração</span></div><div><strong>28</strong><span>atividades na comunidade</span></div><div><strong>03</strong><span>conquistas desbloqueadas</span></div></div><div className="profile-content-grid"><section><div className="section-heading"><div><p className="eyebrow">SUA JORNADA</p><h2>Conquistas</h2></div><button className="text-btn">Ver todas <ArrowRight size={15}/></button></div><div className="achievement-grid"><div className="achievement-card unlocked"><div className="achievement-icon"><Compass size={22}/></div><strong>Explorador</strong><span>Visitou 10 laboratórios</span><small>Desbloqueado em ago 2026</small></div><div className="achievement-card unlocked"><div className="achievement-icon"><Wrench size={22}/></div><strong>Maker</strong><span>Utilizou 5 laboratórios diferentes</span><small>Desbloqueado em jul 2026</small></div><div className="achievement-card locked"><div className="achievement-icon"><UsersRound size={22}/></div><strong>Colaborador</strong><span>Participe de 5 projetos</span><small>3 de 5 concluídos</small></div></div><div className="section-heading profile-project-heading"><div><p className="eyebrow">ATIVIDADE RECENTE</p><h2>Seus projetos</h2></div></div><div className="profile-project-list">{projects.slice(0, 2).map((project) => <div key={project.name}><span className="project-mini-icon"><FolderKanban size={17}/></span><div><strong>{project.name}</strong><span>{project.status} · última atividade há 2h</span></div><ArrowUpRight size={16}/></div>)}</div></section><aside className="profile-side"><div className="panel profile-info"><div className="panel-heading"><h3>Sobre você</h3><button className="icon-btn"><MoreHorizontal size={17}/></button></div><div className="info-line"><span>Curso</span><strong>Engenharia Mecatrônica</strong></div><div className="info-line"><span>Período</span><strong>6º semestre</strong></div><div className="info-line"><span>Campus</span><strong>SENAI CIMATEC</strong></div><div className="info-line"><span>Disponibilidade</span><strong className="available-text"><i className={statusClass("available")}></i>Hoje à tarde</strong></div></div><div className="panel profile-quote"><Sparkles size={18}/><p>“A melhor ideia é aquela que encontra as pessoas e o espaço certos para acontecer.”</p><span>LabMatch · seu espaço para criar</span></div></aside></div></div>; }

function PreferencesPage({ theme, setTheme }: { theme: "light" | "dark"; setTheme: (t: "light" | "dark") => void }) { const [contrast, setContrast] = useState(false); const [reduced, setReduced] = useState(false); const [fontSize, setFontSize] = useState("Padrão"); const [language, setLanguage] = useState("Português"); return <div className="page preferences-page"><div className="page-intro-row"><div><p className="eyebrow">CONTROLE SUA EXPERIÊNCIA</p><h2>Preferências</h2><p className="muted">Ajuste o LabMatch para funcionar do seu jeito.</p></div><span className="saved-pill"><CheckCircle2 size={14}/> Salvo automaticamente</span></div><div className="settings-layout"><div className="settings-nav"><button className="active"><Settings2 size={16}/> Geral</button><button><Accessibility size={16}/> Acessibilidade</button><button><Bell size={16}/> Notificações</button><ShieldCheck size={16}/><span>Privacidade</span></div><section className="settings-content"><div className="settings-group"><div className="settings-group-head"><div><p className="eyebrow">APARÊNCIA</p><h3>Como o LabMatch aparece</h3></div><Eye size={18}/></div><div className="setting-row"><div><strong>Tema</strong><span>Escolha como a interface deve aparecer para você.</span></div><div className="theme-options"><button className={theme === "light" ? "selected" : ""} onClick={() => { setTheme("light"); saveStored("labmatch_theme", "light"); }}><Sun size={16}/> Claro</button><button className={theme === "dark" ? "selected" : ""} onClick={() => { setTheme("dark"); saveStored("labmatch_theme", "dark"); }}><Moon size={16}/> Escuro</button><button onClick={() => toast.info("O tema sistema seguirá as configurações do seu dispositivo.")}><Laptop size={16}/> Sistema</button></div></div><div className="setting-row"><div><strong>Idioma</strong><span>Idioma usado nos textos da interface.</span></div><select value={language} onChange={(e) => { setLanguage(e.target.value); toast.success(`Idioma alterado para ${e.target.value}.`); }}><option>Português</option><option>English</option></select></div></div><div className="settings-group"><div className="settings-group-head"><div><p className="eyebrow">ACESSIBILIDADE</p><h3>Deixe o espaço mais confortável</h3></div><Accessibility size={18}/></div><div className="setting-row"><div><strong>Alto contraste</strong><span>Aumenta a distinção entre textos, bordas e superfícies.</span></div><button className={`switch ${contrast ? "on" : ""}`} onClick={() => setContrast(!contrast)}><i></i></button></div><div className="setting-row"><div><strong>Reduzir movimento</strong><span>Desativa animações não essenciais da interface.</span></div><button className={`switch ${reduced ? "on" : ""}`} onClick={() => setReduced(!reduced)}><i></i></button></div><div className="setting-row"><div><strong>Tamanho do texto</strong><span>Escolha uma escala confortável para leitura.</span></div><div className="font-options">{["Padrão", "Grande", "Maior"].map((size) => <button key={size} className={fontSize === size ? "selected" : ""} onClick={() => setFontSize(size)}><Type size={14}/>{size}</button>)}</div></div></div><div className="settings-group"><div className="settings-group-head"><div><p className="eyebrow">PREFERÊNCIAS INTELIGENTES</p><h3>Para encontrar mais rápido</h3></div><Sparkles size={18}/></div><div className="favorite-chips"><span>Arduino <X size={13}/></span><span>Baixo ruído <X size={13}/></span><span>Entrada acessível <X size={13}/></span><button><Plus size={14}/> adicionar</button></div><p className="setting-help"><Lightbulb size={15}/> Essas preferências ajudam a ordenar seus Match Scores sem limitar sua busca.</p></div></section></div></div>; }

function App() {
  const [splash, setSplash] = useState(() => !sessionStorage.getItem("labmatch_intro_seen"));
  const [logged, setLogged] = useState(() => getStored("labmatch_logged", false));
  const [page, setPage] = useState<Page>("home"); const [collapsed, setCollapsed] = useState(false); const [theme, setTheme] = useState<"light" | "dark">(() => getStored("labmatch_theme", "light")); const [search, setSearch] = useState(""); const [reservations, setReservations] = useState<Reservation[]>(() => getStored("labmatch_reservations", initialReservations)); const [selectedLab, setSelectedLab] = useState<Lab | null>(null); const [bookingLab, setBookingLab] = useState<Lab | null>(null); const [posts, setPosts] = useState<Post[]>(() => getStored("labmatch_posts", initialPosts));
  useEffect(() => { document.documentElement.classList.toggle("dark", theme === "dark"); }, [theme]);
  function cancelReservation(id: string) { const next = reservations.map((r) => r.id === id ? { ...r, status: "cancelled" as const } : r).filter((r) => r.status !== "cancelled"); setReservations(next); saveStored("labmatch_reservations", next); toast.success("Reserva cancelada com sucesso."); }
  function addReservation(reservation: Reservation) { const next = [...reservations, reservation]; setReservations(next); saveStored("labmatch_reservations", next); setBookingLab(null); setSelectedLab(null); setPage("reservas"); toast.success("Reserva confirmada. Até lá, Lucca!"); }
  if (splash) return <Splash onDone={() => setSplash(false)}/>;
  if (!logged) return <><Login onLogin={() => setLogged(true)}/><Toaster position="bottom-right"/></>;
  const common = { onOpenLab: setSelectedLab, onBook: (lab: Lab) => setBookingLab(lab) };
  let content: React.ReactNode;
  if (page === "home") content = <HomePage reservations={reservations} onOpenLab={setSelectedLab} onBook={setBookingLab} setPage={setPage} onCancel={cancelReservation} search={search} setSearch={setSearch}/>;
  if (page === "explore") content = <ExplorePage {...common} search={search} setSearch={setSearch}/>;
  if (page === "map") content = <MapPage onOpenLab={setSelectedLab} setPage={setPage}/>;
  if (page === "reservas") content = <ReservationsPage reservations={reservations} onCancel={cancelReservation} onBook={() => setPage("explore")}/>;
  if (page === "community") content = <CommunityPage posts={posts} setPosts={setPosts}/>;
  if (page === "projects") content = <ProjectsPage setPage={setPage}/>;
  if (page === "events") content = <EventsPage/>;
  if (page === "notifications") content = <NotificationsPage/>;
  if (page === "profile") content = <ProfilePage setPage={setPage}/>;
  if (page === "preferences") content = <PreferencesPage theme={theme} setTheme={setTheme}/>;
  return <div className={`app-shell ${collapsed ? "nav-collapsed" : ""}`}><Sidebar page={page} setPage={setPage} onLogout={() => { saveStored("labmatch_logged", false); setLogged(false); }} collapsed={collapsed} setCollapsed={setCollapsed}/><main className="main-shell"><Topbar page={page} setPage={setPage} theme={theme} setTheme={setTheme}/>{content}</main>{selectedLab && <DetailModal lab={selectedLab} onClose={() => setSelectedLab(null)} onBook={(lab) => { setSelectedLab(null); setBookingLab(lab); }}/>} {bookingLab && <BookingModal lab={bookingLab} onClose={() => setBookingLab(null)} onBooked={addReservation}/>}<Toaster position="bottom-right" richColors/></div>;
}

export default App;
